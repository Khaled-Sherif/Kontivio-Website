# Django backend URL — no trailing slash
# Development
VITE_API_URL=http://localhost:8000

# Production (set this in Vercel dashboard: Settings → Environment Variables)
# VITE_API_URL=https://api.kontivio.de
